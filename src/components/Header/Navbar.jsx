import styles from "./Header.module.css"

const Navbar = () => {

    // en vez de una navbar podría ser un menu hamburguesa. quedaría aesthetic
    return(
        <div className={styles.navbarContainer}>
            magia
        </div>
    )
}

export default Navbar;